{
  "name": "www",
  "version": "1.0.0",
  "scripts": {
    "start": "hugo server",
    "build": "hugo --minify && npm run build:functions",
    "build:functions": "netlify-lambda build assets/lambda",
    "build:preview": "npm run build -D -F",
    "server": "hugo server",
    "clean": "rimraf public resources functions",
    "clean:deps": "node scripts/clean-deps",
    "copy:deps": "node scripts/copy-deps",
    "lint": "npm run -s lint:scripts && npm run -s lint:styles",
    "lint:scripts": "eslint assets/js assets/lambda config",
    "lint:styles": "stylelint \"assets/scss/**/*.{css,sass,scss,sss,less}\"",
    "test": "npm run -s lint"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^2.1",
    "autoprefixer": "^9.7",
    "eslint": "^6.8",
    "ga-lite": "^2.0.5",
    "netlify-lambda": "^1.6",
    "postcss-cli": "^7.1",
    "purgecss-whitelister": "^2.4",
    "rimraf": "^3.0",
    "shelljs": "^0.8.4",
    "stylelint": "^13.2",
    "stylelint-config-standard": "^20.0"
  },
  "dependencies": {
    "bootstrap": "^4.4.1",
    "font-awesome": "^4.7.0",
    "jquery": "^3.5.0",
    "photoswipe": "^4.1.3",
    "popper.js": "^1.16.1",
    "prismjs": "^1.20.0",
    "rrssb": "^1.14.0"
  }
}
